@page
@model AdminReportedMessagesModel
@{
}


<table>
    @foreach (var post in Model.Posts)
    {
        <tr>

            <td>@post.Text</td>
            <td><a asp-route-deleteId="@post.Id">Remove</a></td>
        </tr>

    }
</table>